---
- hosts: webservers
  become: yes

  tasks:
    - name: Установить nginx
      apt:
        name: nginx
        state: present
        update_cache: yes

    - name: Запустить nginx
      systemd:
        name: nginx
        state: started
        enabled: yes
